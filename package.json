{
  "name": "token-usage",
  "private": true,
  "version": "0.1.20",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "run:exe": "node scripts/run-release.mjs",
    "release:publish": "node scripts/publish-release.mjs",
    "test": "cd src-tauri && cargo test --lib && cargo test --test risk_scenarios",
    "test:coverage": "bash scripts/coverage.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
